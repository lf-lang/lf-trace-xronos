/**
 * This test shows the best approach for the user who
 * prefers a clean installation that can be easily removed later.
 * To run this test successfully,
 * install lf-trace-xronos on the system path.
 */
target C {
    tracing: true, // Turn on tracing.
    trace-plugin: true,
    cmake-args: {
        LF_TRACE_PLUGIN: "lf-trace-xronos",
        LF_TRACE_PLUGIN_LIBRARY: "lf::trace-impl",
        LF_TRACE_PLUGIN_PATHS: "../../../install", // Relative to the generated C project directory
    },
    timeout: 5s,
}

import Sensor, Processing, Actuator from "lib/Components.lf"

main reactor {
    s = new Sensor()
    p = new Processing()
    a = new Actuator()
    s.out -> p.in
    p.out -> a.in
}